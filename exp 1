#include <stdio.h>

int main(){
    int n;
    if(scanf("%d",&n)!=1) return 0;
    if(n<0){ printf("0"); return 0; }
    unsigned long long dp[n+1];
    dp[0]=1;
    for(int i=1;i<=n;i++){
        dp[i]=dp[i-1];
        if(i>=3) dp[i]+=dp[i-3];
    }
    printf("%llu",dp[n]);
    return 0;
}
